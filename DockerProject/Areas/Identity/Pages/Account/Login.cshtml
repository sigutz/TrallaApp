@page
@model LoginModel

@{
    ViewData["Title"] = "Log in";
    Layout = "/Views/Shared/_Layout.cshtml";
}
<style>
    /* Add to wwwroot/css/site.css */

    .auth-container {
        min-height: 80vh;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .auth-card {
        width: 100%;
        max-width: 450px;
        border: none;
        border-radius: 1rem;
        box-shadow: 0 10px 25px rgba(0,0,0,0.05);
        background: white;
        overflow: hidden;
    }

    .auth-header {
        background-color: #fff;
        padding: 2rem 2rem 0;
        text-align: center;
    }

    .auth-body {
        padding: 2rem;
    }

    .nav-pills .nav-link {
        color: #6c757d;
        font-weight: 500;
        border-radius: 0.5rem;
        padding: 0.75rem 1.5rem;
    }

    .nav-pills .nav-link.active {
        background-color: #0d6efd; /* Bootstrap Primary */
        color: white;
        box-shadow: 0 4px 6px rgba(13, 110, 253, 0.2);
    }

    .form-floating > .form-control:focus ~ label {
        color: #0d6efd;
    }

    .form-floating > .form-control {
        border-radius: 0.5rem;
        border: 1px solid #dee2e6;
    }

    .btn-auth {
        padding: 0.8rem;
        font-weight: 600;
        border-radius: 0.5rem;
        font-size: 1rem;
    }
</style>
<div class="auth-container">
    <div class="auth-card">
        
        <div class="auth-header">
            <h3 class="mb-4 fw-bold">@ViewData["Title"]</h3>
            <ul class="nav nav-pills nav-fill mb-3">
                <li class="nav-item">
                    <a class="nav-link active" href="#">Log In</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Register</a>
                </li>
            </ul>
        </div>

        <div class="auth-body pt-0">
            <form id="account" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert"></div>

                <div class="form-floating mb-3">
                    <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                    <label asp-for="Input.Email" class="text-muted">Email address</label>
                    <span asp-validation-for="Input.Email" class="text-danger small"></span>
                </div>

                <div class="form-floating mb-3">
                    <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" placeholder="password" />
                    <label asp-for="Input.Password" class="text-muted">Password</label>
                    <span asp-validation-for="Input.Password" class="text-danger small"></span>
                </div>

                <div class="d-flex justify-content-between align-items-center mb-4">
                    <div class="form-check">
                        <input class="form-check-input" asp-for="Input.RememberMe" />
                        <label class="form-check-label text-muted small" asp-for="Input.RememberMe">
                            Remember me
                        </label>
                    </div>
                    <a id="forgot-password" asp-page="./ForgotPassword" class="text-decoration-none small">Forgot password?</a>
                </div>

                <button id="login-submit" type="submit" class="w-100 btn btn-primary btn-auth mb-3">Log In</button>
            </form>

            @if ((Model.ExternalLogins?.Count ?? 0) > 0)
            {
                <div class="text-center mt-3">
                    <p class="text-muted small mb-3">Or continue with</p>
                    <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post">
                        <div class="d-flex gap-2 justify-content-center">
                            @foreach (var provider in Model.ExternalLogins!)
                            {
                                <button type="submit" class="btn btn-outline-secondary btn-sm" name="provider" value="@provider.Name">
                                    @provider.DisplayName
                                </button>
                            }
                        </div>
                    </form>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}